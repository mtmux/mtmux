# ccremote — docker sidecar config
#
# Used by the `nginx` service in docker-compose.prod.yml. Service names
# (web / relay / docs) are resolved via docker's internal DNS.
#
# TLS is bring-your-own-cert: mount your Let's Encrypt directory at
# /etc/letsencrypt and uncomment the HTTPS server block below. Otherwise
# only the HTTP server is active for local / behind-another-proxy use.

upstream ccremote_web {
    server web:14100;
}

upstream ccremote_relay {
    server relay:14300;
}

upstream ccremote_docs {
    server docs:14102;
}

# --- HTTP (also handles ACME http-01 before TLS is issued) ----------------
server {
    listen 80 default_server;
    listen [::]:80 default_server;
    server_name _;

    # certbot HTTP-01 challenge
    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }

    # Relay WebSocket
    location /ws {
        proxy_pass http://ccremote_relay;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;

        proxy_buffering off;
        proxy_read_timeout 3600s;
        proxy_send_timeout 3600s;
    }

    location /docs {
        proxy_pass http://ccremote_docs;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    location / {
        proxy_pass http://ccremote_web;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}

# --- HTTPS (uncomment once certs are mounted) -----------------------------
# server {
#     listen 443 ssl;
#     listen [::]:443 ssl;
#     http2 on;
#     server_name ccremote.example.com;
#
#     ssl_certificate /etc/letsencrypt/live/ccremote.example.com/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/ccremote.example.com/privkey.pem;
#     ssl_protocols TLSv1.2 TLSv1.3;
#     ssl_ciphers HIGH:!aNULL:!MD5;
#     ssl_prefer_server_ciphers on;
#
#     add_header Strict-Transport-Security "max-age=31536000; includeSubDomains" always;
#     add_header X-Frame-Options "SAMEORIGIN" always;
#     add_header X-Content-Type-Options "nosniff" always;
#     add_header Referrer-Policy "strict-origin-when-cross-origin" always;
#
#     location /ws {
#         proxy_pass http://ccremote_relay;
#         proxy_http_version 1.1;
#         proxy_set_header Upgrade $http_upgrade;
#         proxy_set_header Connection "upgrade";
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#         proxy_buffering off;
#         proxy_read_timeout 3600s;
#     }
#
#     location /docs {
#         proxy_pass http://ccremote_docs;
#         proxy_http_version 1.1;
#         proxy_set_header Host $host;
#         proxy_set_header X-Forwarded-Proto $scheme;
#     }
#
#     location / {
#         proxy_pass http://ccremote_web;
#         proxy_http_version 1.1;
#         proxy_set_header Host $host;
#         proxy_set_header X-Forwarded-Proto $scheme;
#     }
# }
